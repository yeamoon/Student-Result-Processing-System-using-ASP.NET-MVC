@using FirstMVCApplication.Models;
@model List<FacultyViewModel>

@{
    ViewBag.Title = "FacultyList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>FacultyList</h2>


<div class="ibox float-e-margins">
    <div class="ibox-title">
        <div class="row">
            <div class="col-lg-6">
                <h3>Student List</h3>
            </div>
            <div class="col-lg-6">
                <div align='right' style="margin-top: 10px;">

                    <a target='_blank' href='@Url.Action("Faculty", "Faculty", new {id = 0})' class="btn btn-primary btn-smaller">New Faculty</a>
                </div>
            </div>
        </div>
    </div>
    <div class="ibox-content">
        <table class="table table-striped table-bordered table-hover dataTables">
            <thead>
                <tr>
                    
                    <th>
                        @Html.DisplayNameFor(m => m.FirstOrDefault().Name)
                    </th>
                    
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        
                        <td>
                            @item.Name
                        </td>
                       
                       
                        <td>
                            <a href='@Url.Action("Delete", "Faculty", new {id = @item.Id})'>Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

